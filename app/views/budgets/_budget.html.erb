<h1 class="text-xl text-white text-center font-bold mb-8">💸 <%= budget.name %></h1>
<div id="<%= dom_id budget %>" class="flex flex-col-reverse lg:flex-row w-full text-center border-slate-700 border-2 rounded mb-8">

  <div class="h-80 w-full lg:w-1/2 p-2 flex items-center justify-center">
    <% if budget.spending_plan %>
    <% else %>
      <%= link_to 'create spending plan', new_budget_spending_plan_path(@budget), class: "h-fit bg-purple-500 hover:bg-purple-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:border-purple-500" %>
    <% end %>
  </div>
  <div class="flex justify-center items-center h-80 w-full lg:w-1/2 bg-slate-700">
    <% if budget.spending_plan %>
    <% else %>
      <p class="text-slate-200 bg-slate-900 text-6xl rounded-full h-40 w-40 flex justify-center items-center">$0</p>
    <% end %>
  </div>
</div>
